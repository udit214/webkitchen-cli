[project]
name = "webkitchen"
version = "0.1.0"
dependencies = ["typer"]

[project.scripts]
wk = "webkitchen.cli:app"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"